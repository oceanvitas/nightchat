#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tiny/bin/tiny" "$@"
  ret=$?
else 
  node  "$basedir/../tiny/bin/tiny" "$@"
  ret=$?
fi
exit $ret
